pluginManagement {
    repositories {
        google()               // for Android plugins
        mavenCentral()
        gradlePluginPortal()   // fallback for other plugins
    }
}
rootProject.name = "tauqeet-kmp"
include(":shared")
